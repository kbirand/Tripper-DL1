.DS_Store
build/
*.bin
# ...except released app images, kept in the repo so they can be AirDropped
# to a phone and flashed at the bike with no toolchain. See docs/flashing.md.
!releases/*.bin
.venv/
__pycache__/
*.asc
*.blf
